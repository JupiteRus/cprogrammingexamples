#include <stdio.h>

int main(){

int a[20];
a[1]=1;
a[0]=1;

printf("a[0] = %d\n ",a[0]);            //a[0] değerinide ekranada görebilmek için yazdım.

    for(int i = 1;i<21;i++){
        a[i+1] = a[i] + a[i-1];
        printf("a[%d] = %d\n",i,a[i]);

    }

}
